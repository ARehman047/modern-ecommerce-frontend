// Only keeping category filters for simplified frontend
export const filters = [
  // No additional filters needed - category is handled separately
];

export const singleFilter = [
  // All other filters removed as requested
];
